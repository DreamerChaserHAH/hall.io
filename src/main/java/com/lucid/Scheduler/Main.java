package com.lucid.Scheduler;

import java.util.Date;
import java.util.Scanner;
//import com.hallio.dms.DatabaseManager; // Import DatabaseManager

public class Main {
    public static void main(String[] args) {
      //  HallManager hallManager = new HallManager(); // Create HallManager instance
        // Ensure the database is created or initialized properly
        //new HallManagementGUI(hallManager); // Launch the GUI
    }
}
